import React from 'react'

export default function UserLayout({ children }) {
    return (
        <div>
            {/**header ở đây */}
            {children}
            {/**footer ở đây */}
        </div>
    )
}
